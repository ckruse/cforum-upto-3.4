

PERL = @PERL@
libdir = @libdir@
PERLMODULES = @PERLMODULES@

all:
	cd CForum-Clientlib && $(PERL) Makefile.PL LIB=$(PERLMODULES)
	cd CForum-Configparser && $(PERL) Makefile.PL LIB=$(PERLMODULES)
	cd CForum-Template && $(PERL) Makefile.PL LIB=$(PERLMODULES)
	cd CForum-Validator && $(PERL) Makefile.PL LIB=$(PERLMODULES)

	cd CForum-Clientlib && make
	cd CForum-Configparser && make
	cd CForum-Template && make
	cd CForum-Validator && make

install:
	cd CForum-Clientlib && make install
	cd CForum-Configparser && make install
	cd CForum-Template && make install
	cd CForum-Validator && make install

clean:

distclean:

maintainer-clean:

# eof