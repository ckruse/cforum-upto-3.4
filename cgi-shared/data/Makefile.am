
SUBDIRS=messages

install-data-local:
	if test ! -d $(with_archive_dir); then \
	  $(MKDIR) -p $(with_archive_dir); \
	fi; \
	if test ! -d $(with_message_dir); then \
		$(MKDIR) -p $(with_message_dir); \
	fi

