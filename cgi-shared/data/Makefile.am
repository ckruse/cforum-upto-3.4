
SUBDIRS=messages

install-data-local:
	if ! -d $(with_archive_dir); then \
	  $(MKDIR) -p $(with_archive_dir); \
	fi; \
	if ! -d $(with_message_dir); then \
		$(MKDIR) -p $(with_message_dir); \
	fi

